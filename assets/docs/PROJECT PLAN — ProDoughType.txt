PROJECT PLAN — ProDoughType vNext (V1)

Current state and immediate fixes
0a) Step 1 Foundation not showing on initial load. Likely CSS block for .step/.stepbody/.field got trimmed; restore minimal styles so Step 1 renders.
0b) Templates click error fixed by adding a small toast() helper in ui.js and calling toast("…") directly.
0c) Template cards are “square image on top + text beneath” unified card; grid sizing tuned for 2–4 per row.

Preferences (Prefs) upgrades
1a) Add “Fermentation Model” selection to Preferences. This becomes the default model used when user hits “Auto Ferment” (later).
1b) Keep the fermentation models as a list in prefs (so we can add more without changing UI).
1c) Add toggles in prefs for: warnings on/off, (later) affiliate links, (later) ad slot visibility.

Flour section placement and wiring
2a) Move the Flour “template browser/grid” from the DB route into the calculator as Step 2: Flour.
2b) Step 2 Flour becomes the only way to select flour(s). The DB route can still exist, but it’s for browsing/reference, not selection.
2c) Clicking a flour opens a focused popup (modal) with specs and only two actions: Use Flour / Close. Close also works by clicking outside.
2d) Use Flour sends it to the calculator blend:

if empty: sets 100%

if already has flours: add and auto-split %
2e) Show weighted protein + absorption in Step 2 and also in Live Preview.

Reusable “Browser Panel” format (Templates / Flour / KB)
3a) Create one reusable browser layout: left categories, top bar (search/sort), grid, optional detail popup.
3b) Templates browser and Flour browser both use the same layout component, different datasets.
3c) Keep cards simple: image + name; details in popup, not on the tile.
3d) Standardize asset paths and naming so the browser can load them without special cases.

App start behavior: force style selection or not
Decision needed for V1:
4a) Option A (recommended): allow starting in Dough with a default “NY Round” style loaded silently. User can change it anytime.
4b) Option B: on first launch, show Templates screen and require choosing a style before using calculator.
4c) If we do B, add “Skip / Use default” so advanced users don’t get slowed down.

Templates wiring (V1)
5a) Clicking a Style Template loads calculator defaults:

shape, size defaults, TF range start, hydration start, surface, balls

preferred flour category suggestion (not forced)
5b) Tribute Templates later: same structure, plus disclaimer and “community interpretation” label.

Fermentation
6a) Move “Fermentation model button” off sidebar; it belongs in Prefs.
6b) Ferment page remains for now, but “auto ferment” uses the prefs-selected model.
6c) V1: placeholders only, but the selection plumbing exists (prefs → session default).

Mobile app direction (Phase 2)
7a) “Reminder-only” mobile app first (not full calculator).
7b) Site generates a QR/JSON “BakePlan” payload: recipe summary + timeline reminders.
7c) Mobile app reads QR and schedules reminders locally.
7d) Keep QR button in Live Preview; enable for testing even before full logic is finalized.

File/folder structure (keep it simple)
8a) /assets/templates/*.svg for style template art (already started).
8b) Later: /assets/flours/brands/*.svg and /assets/flours/products/*.webp (not needed for V1).
8c) Keep UI icons separate: /assets/icons/*.svg.
8d) Keep datasets separate: /data/templates.json, /data/flours.json, /data/kb/*.json (can be phased in).

Next actions (do these next, in order)
9a) Fix Step 1 initial render (restore CSS).
9b) Build “Browser Panel” as a reusable component.
9c) Move Flour browser into Step 2 and wire “Use Flour” → blend.
9d) Add Fermentation Model selector into Prefs and store it.
9e) Decide startup flow (default style vs forced template selection).
9f) Standardize template/flour data objects so everything is plug-in.